# Skill Definitions

Place skill definition files here. Skills are reusable task templates
that can be invoked by name.

## Format

Skills follow the Claude Code skill format — Markdown files with
optional YAML frontmatter defining parameters and behavior.
